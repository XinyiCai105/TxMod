name: txmod
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.11
  - numpy>=1.21
  - pandas>=1.3
  - scipy>=1.7
  - matplotlib>=3.5
  - pyfaidx>=0.7
  - pytest>=7
  - pip
  - pip:
      - tensorflow>=2.10   # required only for iM6A m6A prediction
      - -e .
